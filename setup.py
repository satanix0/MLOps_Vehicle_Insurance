from setuptools import setup, find_packages
"""
This script is used to set up the packages created within src, int our virtual env. It uses setuptools to define the package metadata and configuration.
"""

setup(
    name="src",
    version="0.0.1",
    author="Piyush",
    author_email="piyushkr172@gmail.com",
    packages=find_packages()
)
